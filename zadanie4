#include <stdio.h>

int jedzonko(int n) {
    int cos = 0;
    int resztazdzielenia, l=1;
    while (k != 0) {
        resztazdzielenia = n%2;
        k=k/2;
        cos += resztazdzielenia*l;
        l*=10;
    }
    return cos;
}
int main(){
  printf("Podaj liczbe ktora chcesz zamienic: ");
  int k;
  scanf("%d", &k);
  printf("%d = %d\k", k, jedzonko(k));
  return 0;
}
